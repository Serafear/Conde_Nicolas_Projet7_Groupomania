export const SET_POSTS = (state, Posts) => {
    state.Posts = Posts;
}

export const SET_COMMENTS = (state, Comments) => {
    state.Comments = Comments;
}

export const SET_REACTIONS = (state, isLike) => {
    state.isLike = isLike;
}

export const SET_Like = (state, Like) => {
   state.Like = Like
}


